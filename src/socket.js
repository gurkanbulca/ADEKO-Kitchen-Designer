// SOCKET.IO
        // socket.on('put mesh', meshInfo => {
        //     console.log(meshInfo);
        //     parameters.placedMeshes.push(createMeshFromMeshInfo(meshInfo));
        // });
        // socket.on('delete mesh', index => {
        //     console.log(index);
        //     parameters.placedMeshes[index].dispose()
        //     parameters.placedMeshes.splice(index, 1);
        // })